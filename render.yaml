services:
  - type: web
    name: ev-transition-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: FRONTEND_ORIGIN
        value: https://dev.jakamarinsek.com
      - key: PREDICTION_TIMEOUT_MS
        value: "30000"
